import 'package:flutter/material.dart';

class CoursesDP extends StatelessWidget {
  const CoursesDP({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}